[pytest]
pythonpath = .
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --strict-markers
asyncio_default_fixture_loop_scope = function
markers =
    unit: Unit tests (no DB, all mocked)
    integration: Integration tests (requires PostgreSQL on port 5433)